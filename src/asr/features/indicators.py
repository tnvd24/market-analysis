"""Phase 3: deterministic indicators (RSI, MACD, MAs, ATR, Bollinger).
Real math via pandas-ta — never LLM-guessed. Reads `candles`, writes `features`."""
# TODO(phase3): implement compute_features(df) -> features_df with unit tests.
