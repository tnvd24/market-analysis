Let's build it. Here's the full plan for a grounded, research-only agentic stock research system for Indian markets — the architecture we landed on: deterministic data and math on the outside, Claude only for reasoning and synthesis inside, GCP-flavored, containerized on your CachyOS box.
I've tagged every step with an owner so we can split cleanly:

[PRASAD] — needs Prasad: accounts, credentials, money, decisions, running commands on the machine
[CLAUDE] — Claude produces it: code, Dockerfiles, schemas, configs, docs
[BOTH] — Claude writes it, Prasad runs/verifies it


Phase 0 — Scope & decisions (unblocks everything)

[PRASAD] Confirm research-only boundary (no automated order placement). This keeps you entirely outside SEBI's Algo-ID mandate. Everything below assumes this.
    -> CONFIRMED. Research-only; no order-placement path anywhere in the system.
[PRASAD] Pick the data/broker source — this is the one choice that shapes the most code (auth flow differs per broker).
    -> DECIDED: Upstox API (read-only Analytics Token preferred).
[PRASAD] Pick the stock universe (e.g. Nifty 50, a custom watchlist, or broader).
    -> DECIDED: Nifty 500.
[PRASAD] Create a GCP project with billing enabled; get an Anthropic API key.
    -> PENDING (Prasad). Not needed until Phase 4 (Anthropic key) / Phase 8-9 (GCP). DuckDB-first means we build Phases 2-3 with zero cloud cost.
[CLAUDE] Recommend a dev strategy that keeps cost near zero until it works (local DuckDB + Parquet first, lift to BigQuery later).
    -> DECIDED: DuckDB + Parquet locally now; BigQuery in prod behind the same StorageAdapter interface (flip STORAGE_BACKEND). Full rationale in docs/decisions.md.

Phase 0 decisions log: docs/decisions.md

Phase 1 — Scaffolding & environment

[CLAUDE] Repo layout: a `src/asr/` Python package (src-layout — cleaner on GitHub and packages cleanly into a multi-stage Docker image) holding the functional areas ingest/, features/, news/, agents/, backtest/, storage/, app/ (Phase 7 UI); infra/ sits at the repo root (deploy manifests, Secret Manager, DAGs — Phases 8-9). Dependency setup (uv), pre-commit, ruff linting.
[CLAUDE] .env.example + config module; local secrets via .env, prod via Secret Manager.
[CLAUDE] Multi-stage Dockerfile (Python + Node.js, since any MCP piece needs Node) and a docker-compose.yml for Rancher/Docker on CachyOS.
[PRASAD] docker compose up to confirm the shell container builds and runs on your machine.

Phase 2 — Data ingestion (deterministic)

*** SUPERSEDED: the price source is now NSE BHAVCOPY, not Upstox. No token anywhere. ***
    Kite has no news endpoint and Upstox's candle adjustment is undocumented; NSE bhavcopy is
    the file both derive from. One request = the whole market for a day. Crucially, NSE also
    publishes corporate actions, so we ADJUST for splits/bonuses ourselves, deterministically,
    instead of trusting a broker. src/asr/ingest/{bhavcopy,corporate_actions,adjust,prices}.py
    Live: 58,435 candles / 117 days / 1,977 actions / 0 errors. See docs/decisions.md.

[CLAUDE] Broker API client wrapper (auth + rate limiting + retries) for the source you picked.
    -> RETIRED. The Upstox market-data client is gone; upstox_client.py now only holds the
       (auth-free) instrument master + error types used by the optional Upstox news feed.
[CLAUDE] Ingestors: historical OHLCV backfill, daily incremental, fundamentals, corporate actions.
    -> DONE (OHLCV): src/asr/ingest/ohlcv.py — backfill + daily incremental, both idempotent
       (upsert on instrument_key+ts), so re-runs never duplicate.
    -> DONE (corporate actions): NSE publishes them. src/asr/ingest/corporate_actions.py.
    -> DEFERRED: fundamentals. Still no free source; doesn't block Phases 3-6.
    -> RESOLVED (was: are candles split-adjusted?): we no longer depend on anyone else's
       adjustment. Bhavcopy is raw, and we adjust from NSE's corporate-action feed ourselves.
       Splits/bonuses adjust; dividends are recorded but don't; rights are WARNed, not
       adjusted; an unreadable ratio is an ERROR and is never guessed at.
[CLAUDE] Storage adapter that writes to DuckDB locally and BigQuery in prod behind the same interface.
    -> DONE. candles + instruments tables; upsert_candles / upsert_instruments /
       latest_candle_ts on both adapters.
[PRASAD] Authorize the broker (e.g. Kite needs a daily token handshake), run the initial backfill, eyeball the data.
    -> TODO (Prasad): put the Upstox Analytics Token in .env, then run
       `asr ingest backfill` and `asr ingest status`. Universe resolution already works
       (all 500 Nifty constituents resolve; no token needed for that step).

Phase 3 — Feature / indicator layer (deterministic)

[CLAUDE] Compute RSI, MACD, moving averages, ATR, Bollinger, volume features with pandas-ta — real math, never LLM-guessed (this was the repo's core flaw).
    -> DONE. src/asr/features/indicators.py: RSI-14, MACD(12/26/9), SMA-20/50/200, EMA-20,
       ATR-14, Bollinger(20,2), OBV, 1d return, 20d volume average, relative volume.
       Stable column names (macd_hist, bb_lower) so pandas-ta's parameter-derived names
       never leak into agent tool queries. Grouped per instrument — no window spans two stocks.
[CLAUDE] Unit tests asserting indicator correctness against known values.
    -> DONE. tests/test_indicators.py asserts against math computed independently of
       pandas-ta: closed forms (SMA of 1..60), analytic edge cases (RSI = 100 on an
       unbroken uptrend, 0 on a downtrend, 50 on symmetric moves), definitional identities
       (MACD = EMA12-EMA26; hist = MACD-signal), and a constant-true-range ATR.
[BOTH] Run the feature job, spot-check a few tickers.
    -> TODO (Prasad): after the backfill, `asr features build` then
       `asr features show RELIANCE`. Needs the Upstox token first (Phase 2).

Phase 3 notes: features are recomputed in full per instrument, not appended incrementally —
EMA/RSI carry state from every prior bar, so a partial update would quietly disagree with a
full recompute. Warmup rows are NULL, never 0.

Phase 4 — News & sentiment (LLM, grounded)

[PRASAD] Choose/authorize a legit news source (broker news feed, a news API, or curated RSS — not open-web scraping).
    -> DECIDED: two sources. (1) NSE corporate announcements = PRIMARY (the company's own
       filing to the exchange; no auth; verified live). (2) Upstox News API = SECONDARY
       (journalism; needs the token). Kite was considered and rejected: Kite Connect has
       NO news endpoint, and adopting it would add a daily token handshake plus an
       order-capable session. No open-web scraping anywhere.
[CLAUDE] News fetch layer (deterministic, no LLM).
    -> DONE. src/asr/news/{nse,upstox_news,fetch,schema}.py -> `news` table.
       Dedup on a content id (news windows always overlap). `asr news fetch` / `asr news show`.
[CLAUDE] Claude-based extractor that turns articles into structured JSON (sentiment, catalyst type, entities) against a Pydantic schema, with dedup and timestamps.
    -> DROPPED. Superseded by the two-track design (see docs/decisions.md): news is collected
       and shipped VERBATIM in the research pack, and a human does the interpretation in a
       Claude chat on the subscription. No API key, no spend, no hallucination surface.
       Reinstate only if a truly unattended (nobody-at-the-keyboard) brief is ever wanted.
[CLAUDE] Research-pack exporter + data-quality assertion layer.
    -> DONE. src/asr/quality/checks.py (turn silent corruption into loud errors),
       src/asr/features/signals.py (rule-based, evidence-carrying), src/asr/pack/build.py
       (JSON/Markdown pack), prompts/analysis.md (the paste-in prompt).
       `asr quality` / `asr pack build SYMBOL`.

Phase 5 — Interactive analysis (was: automated agent synthesis)

[CLAUDE] A reusable analysis prompt/template you paste the research pack into.
    -> DONE: prompts/analysis.md. Forbids adding facts from memory, requires citing the
       pack's numbers, refuses rather than guesses, reads data-quality first, surfaces
       contradictions between signals.
[PRASAD] Run `asr pack build RELIANCE`, paste into Claude with the prompt, see if the read
    is useful. That feedback shapes what the pack should carry next.

Phase 5 (deferred) — Automated agent synthesis
    -> The LangGraph supervisor / tool-calling agents below are OPTIONAL now, and only
       needed for a hands-off brief. Deferred, not cancelled.

Phase 5 — Agentic reasoning layer (Claude)

[CLAUDE] LangGraph supervisor + constrained tool-calling agents (screener, technical-context, fundamental-context, news-context, synthesis).
[CLAUDE] Tools query BigQuery/DuckDB with typed inputs — agents read grounded numbers, they don't invent them.
[CLAUDE] ChatAnthropic wiring, structured Pydantic outputs, and guardrails: the model must cite the data rows behind any claim and refuse when data is missing rather than hallucinate a price target.

Phase 6 — Backtest & eval harness (deterministic)

[CLAUDE] vectorbt/backtrader harness over historical data so any rule-based signal has measured performance.
[CLAUDE] A separate eval that checks the agents' reasoning uses grounded data correctly (not P&L — reasoning fidelity).

Phase 7 — Delivery / UI

[CLAUDE] A daily research brief generator (Markdown/PDF) and/or a Streamlit dashboard — read-only research output with disclaimers baked in.

Phase 8 — Orchestration

[CLAUDE] Pipeline DAG (ingest → features → news → research brief). Given your background, Cloud Composer/Airflow will feel native; I'll also give a lighter Cloud Scheduler → Pub/Sub → Cloud Run variant.
[PRASAD] Deploy and schedule it.

Phase 9 — Containerize & deploy

[CLAUDE] Finalize images, push to Artifact Registry, deploy target (Cloud Run for the app, or GKE — and you can mirror it on local Rancher k8s if you want).
[PRASAD] Run the deploy, wire up secrets in GCP.

Phase 10 — Observability, cost control, hardening

[CLAUDE] Structured logging, Claude API spend caps + token budgeting, data-quality checks (Great Expectations), retry/backoff, secret rotation.

Phase 11 — Compliance checkpoint

[BOTH] Document the research-only boundary, confirm no order-placement path exists, and put the "not investment advice" disclaimer in every output.
